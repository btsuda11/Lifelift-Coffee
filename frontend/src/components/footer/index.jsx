import './Footer.css';

const Footer = ({ closeCart }) => {
    return (
        <footer onClick={closeCart}>
            <div>
                <img />
            </div>
        </footer>
    )
}

export default Footer;